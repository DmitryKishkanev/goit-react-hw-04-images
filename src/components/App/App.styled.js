import styled from '@emotion/styled';

const Container = styled.div`
  display: grid;
  place-items: center;
  grid-template-columns: 1fr;
  grid-gap: 16px;
  padding-bottom: 24px;
`;

export { Container };
